import { StyleSheet, Button, Text, View } from 'react-native';

export default function ProcurarScreen({ navigation }) {
	return (
		<View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
			<Text>Procurar Screen</Text>
		</View>
	);
}
